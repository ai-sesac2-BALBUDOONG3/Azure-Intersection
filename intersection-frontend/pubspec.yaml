name: intersection
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  file_picker: ^10.3.6
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  flutter_web_auth:
    path: local_plugins/flutter_web_auth
  # ✅ 새로 추가 (이 2줄만 추가!)
  image_picker: ^1.0.4
  image_gallery_saver: ^2.0.3
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  url_launcher: ^6.2.2
  # ✅ 채팅 시간 포맷용 추가
  intl: ^0.19.0
  emoji_picker_flutter: ^1.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
